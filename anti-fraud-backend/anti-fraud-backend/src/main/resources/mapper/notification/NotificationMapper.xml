<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anti.fraud.modules.notification.mapper.NotificationMapper">
    
    <select id="selectUserNotifications" resultType="com.anti.fraud.modules.notification.entity.Notification">
        SELECT * FROM notification 
        WHERE user_id = #{userId} 
        AND deleted = false
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY create_time DESC
    </select>
    
    <select id="countUnreadNotifications" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM notification 
        WHERE user_id = #{userId} 
        AND read = false 
        AND deleted = false
    </select>
    
    <update id="markAsRead">
        UPDATE notification 
        SET read = true 
        WHERE user_id = #{userId} 
        AND id IN (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
        )
    </update>
    
</mapper>
